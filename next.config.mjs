/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/assignment1",
  assetPrefix: "/assignment1/",
  images: {
    unoptimized: true
  }
};

export default nextConfig;
